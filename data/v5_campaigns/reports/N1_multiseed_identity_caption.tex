\textbf{Multi-realization identity of the dynamically selected bottleneck.} (a) For all 20 disorder realizations, the motif whose memory-connected stationary branch survives up to the largest coupling coincides with the slowest motif at cycle death ($20/20$). Code indices have been shifted to the paper convention $\mu=1,\ldots,P$. (b) Dynamic critical couplings (bracket midpoints; error bars show bracket half-widths) compared with the exact fold of the stationary branch reached after arrest. Filled squares denote realizations in which this branch differs from the memory-connected branch; for seed 43 the arrest-branch augmented solve is unresolved and the memory fold is shown as an open triangle. The lower panel shows residuals relative to both the memory fold and the arrest-reached fold. At resolution $10^{-3}$, 20/20 branch-aware critical values agree (19/20 when only the memory fold is used).
