# Core requirements. Python >= 3.10.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
tqdm>=4.65

# Optional, Apple silicon only: GPU-accelerated pattern projections.
# Every code path has a NumPy float64 fallback, and the N** verification
# campaigns are float64 CPU only by design.
# mlx>=0.15 ; sys_platform == "darwin" and platform_machine == "arm64"

# Optional, for the HTML rendering of the worklogs (code/core/build_report_html.py).
# markdown>=3.5
